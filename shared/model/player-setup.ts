export interface PlayerSetup {
    name: string;
    gridSize: number;
    playerCount: number;
    /** advanced settings for registered users */
    symbol?: string;
    /** advanced settings for registered users */
    color?: string;
}